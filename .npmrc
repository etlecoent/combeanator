# Avoid installing packages published less than 7 days ago — reduces risk of installing
# corrupted or malicious packages that haven't been vetted by the community yet.
min-release-age=7

# Skip lifecycle scripts (postinstall, etc.) to prevent arbitrary code execution
# from a compromised package during install.
ignore-scripts=true
